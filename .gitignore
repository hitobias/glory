# Environment
.env

# WordPress uploads (managed separately)
wp-content/uploads/

# Node
node_modules/
wp-content/themes/glory-theme/node_modules/

# Build output (dist/ is committed for production deployment)

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
docker/mysql/slow.log

# Migration data (large binary files)
data/migration/*.wpress
data/migration/*.sql
data/migration/*.xml

# Export data (local → production migration)
data/export/

# Production env (contains real secrets)
.env.production

# Database seed files (contain real user data — NEVER commit)
docker/*/seed/*.sql.gz
docker/*/seed/*.sql

# Docker volumes (data is in named volumes)
